$kind: http-request
name: Sign In
description: Take the User input from the request body and search for the right user, compare the passwords and return the userId and session Id, among other parameters.
url: http://localhost:3000/auth/sign-in/email
method: POST
headers:
  Origin: http://localhost:8080
body:
  type: json
  content: "{\r

    \    \"email\":\"john.doe@example.com\",\r

    \    \"password\":\"password1234\"\r

    }"
order: 3000
